/**
 * Moved to `components/common/IllustrativeDataChip.jsx` when AMISA needed the
 * same trust furniture. Re-exported from the old path so the Oil & Gas cards
 * that import it here keep working; new code should import from common.
 */
export { default, ProvenanceLine } from '../common/IllustrativeDataChip';
